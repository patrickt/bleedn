# bleedn

### it's bleedin' fast

This is a Rust wrapper for [edn.c](https://github.com/DotFox/edn.c), a SIMD EDN parsing library. It uses Rust idioms to provide memory and type safety with minimal overhead. All functions that perform internal copies include zero-copy equivalents alongside.

### LICENSE

MIT, same as the upstream library.
